[Service]
# Vector keeps a descriptor for each immutable result file it has discovered.
# The ready cache is intentionally preserved, so reserve safe headroom while
# delivery-confirmed archival is designed separately.
LimitNOFILE=65536
